[project]
name = "ai-codeing-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "samkhandani", email = "Sam.khandani.1387@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "bootdev>=0.2.10",
    "dotenv>=0.9.9",
    "google-genai==1.12.1",
    "openai>=2.50.0",
    "python-dotenv==1.1.0",
]

[project.scripts]
ai-codeing-agent = "ai_codeing_agent:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
